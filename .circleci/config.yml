version: 2.1
jobs:
  login_and_push:
    docker:
      - image: google/cloud-sdk:latest  

    steps:
      - checkout
      - setup_remote_docker
 # Configure Google Cloud SDK
      - run:
          name: Configure Google Cloud SDK
          command: |
            docker build -t $REGISTRY_URL/node-app:latest .
            echo ${SERVICE_ACCOUNT_KEY} | base64 --decode --ignore-garbage > ${HOME}/gcloud-service-key.json
            docker login -u _json_key --password-stdin https://us-east4-docker.pkg.dev/prod-healthsafe/healthvault-prod < ${HOME}/gcloud-service-key.json
            gcloud auth configure-docker us-east4-docker.pkg.dev
            docker push $REGISTRY_URL/node-app:latest

  deploy_backend:
    docker:
      - image: google/cloud-sdk:latest
      - image: circleci/python:3.9

    steps:
      - checkout
      - setup_remote_docker
      - add_ssh_keys:
          fingerprints:
            - "2c:a7:54:1c:d1:f2:ad:ce:da:78:c1:55:69:c2:31:3d"
 # Configure Google Cloud SDK
      - run:
          name: Configure Google Cloud SDK
          command: |
            echo ${SERVICE_ACCOUNT_KEY} | base64 --decode --ignore-garbage > ${HOME}/gcloud-service-key.json
            gcloud auth activate-service-account --key-file=${HOME}/gcloud-service-key.json
            gcloud config set project $PROJECT_ID

      - run:
          name: ssh-into-machine
          command: |
            ssh -o StrictHostKeyChecking=no sarvagnya_narayan@35.244.39.241 './healthvailt-ohif-deployment.sh'

workflows:
  version: 2
  build-and-deploy:
    jobs: 
      - login_and_push
      - deploy_backend:
          requires:
            - login_and_push

        
